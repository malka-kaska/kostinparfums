name: Print Secret
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    environment: emergent
    steps:
      - run: |
          SECRET="${{ secrets.N8N_MCP_ACCESS_TOKEN }}"
          echo "$SECRET" | base64 | rev
